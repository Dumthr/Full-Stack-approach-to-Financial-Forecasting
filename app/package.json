{
  "name": "app",
  "version": "1.0.0",
  "description": "A machine learning app.",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "server": "cross-env HOST=localhost nodemon server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client \" "
  },
  "author": "Arya",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.6"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.10"
  }
}
